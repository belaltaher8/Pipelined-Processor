module my_12bit_register(input_data, clock, output_data, clear);

	input [11:0] input_data;
	input clock, clear;
	
	output [11:0] output_data;
	
	wire enableWire;
	
	genvar i;
	generate
	for(i = 0; i < 12; i = i+1) begin: flipFlopLoop
		dflipflop curr_dff(input_data[i], clear, clock, output_data[i]);
	end
	endgenerate
	
endmodule
	
	
	